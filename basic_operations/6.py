# To what value does the following expression evaluate?

print('foo' == 'Foo')

# This expression evaluates to `False` because the `==` operator checks
# if two objects are equal in value, exactly, so case matters.

# SOLUTION
# It evaluates as False since the case matters when comparing strings.