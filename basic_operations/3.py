# What does the following code do? Why?

print('5' + '10')

# This code prints:

# ```
# 510
# ```

# Both objects are strings, not integers, so using the `+` operator
# on them concatenates them as strings, joining them 