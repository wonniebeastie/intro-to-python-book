# Refactor the code from the previous exercise to use coercion to print 
# `15` instead.
print(int('5') + int('10')) # 15

# Using the `int` constructor explicitly coerces the strings to integers
# and adds them together.