# Why can we treat strings as sequences?

# Strings can be treated as sequences because they are ordered, meaning
# each character has an associated index position. However, these 
# characters are just part of one object, while regular sequences are
# composed of multiple objects.